You are an expert in agent behavior analysis and are tasked with analyzing agent behaviors and human feedback
to group similar positive and negative behaviors together, so that you can extract the metrics to evaluate the agents.

Here are the behaviors and feedbacks you need to analyze:

{% for behavior_feedback in behavior_feedback_list %}

{{ loop.index }}
Behavior: {{ behavior_feedback.behavior }}
Feedback: {{ behavior_feedback.feedback }}

{% endfor %}

Instructions:
1. Analyze the behaviors and corresponding human feedback carefully
2. Group similar behaviors together, please make sure that the granularity of the grouping is minimal. Only very similar behaviors should be grouped together.
3. Output a list of metrics, where each metric is composed of
    - A list of good behaviors (sentences describing the behavior)
    - A list of bad behaviors (sentences describing the behavior)
    - An explanation of the metric's meaning and how the positive behaviors should be determined in novel agent behaviors
    - A name for the metric
4. Make sure to include each behavior in at least one metric

N.B.
The granularity of the grouping should be minimal, only very similar behaviors should be grouped together. But don't limit to one particular website:
    - Good name: Location Query Correctness
    - Bad name: Mapping Site Usage Correctness
    - Bad name: Arxiv Categories Correctness
    - DO NOT add the website name in the metric name including huggingface, arxiv, dictionary, ESPN, Apple, Amazon, BBC, etc.
Also, don't limit to one particular character:
    - Good name: Don't settle for less in negotiations
    - Bad name: Oliver's negotiation strategy

Output EXACTLY 5 NEW metrics based on the provided aspects that are NOT already covered by existing metrics.

🚨 CRITICAL CONSTRAINT ENFORCEMENT 🚨
- You MUST generate exactly 5 metrics - no more, no less
- Generating fewer than 5 metrics is a FAILURE
- Even if some metrics seem similar, create 5 distinct metrics
- Do NOT be "smart" and decide fewer metrics are sufficient
- FOLLOW THE EXACT COUNT REQUIREMENT: 5 metrics


IMPORTANT: The provided aspects have already been filtered to exclude behaviors covered by existing metrics. Focus ONLY on creating new metrics from these filtered aspects. Do not duplicate or overlap with existing metric behaviors.

IMPORTANT: The provided aspects have already been filtered to exclude behaviors covered by existing metrics. Focus ONLY on creating new metrics from these filtered aspects. Do not duplicate or overlap with existing metric behaviors.

CRITICAL: Do NOT create metrics with these existing names: "Strategic Planning and Spatial Reasoning", "Winning Rule Comprehension and Creation", "Goal Pursuit and Target Awareness", "Environmental Interaction and Obstacle Navigation". Use completely different names for your new metrics.

🚫 FORBIDDEN NAMES: Do NOT create metrics with these existing names: "Strategic Rule Formation Prioritization", "Strategic Planning and Spatial Reasoning", "Adaptive Learning and Persistence", "Movement Efficiency and Directness", "Winning Rule Comprehension and Creation", "Rule System Flexibility and Alternative Strategy Generation", "Goal Pursuit and Target Awareness", "Environmental Interaction and Obstacle Navigation". Use completely different names for your 5 new metrics.

⚡ FINAL REMINDER: Your response must contain EXACTLY 5 metrics in the JSON output. Count them before responding.
